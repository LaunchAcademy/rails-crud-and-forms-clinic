<h2> All Journals</h2>

<div class="align-center grix-x journals-container">
  <ul>
    <% @journals.each do |journal| %>
      <li>
        <%= link_to(journal.name, journal_path(journal)) %>
      </li>
    <% end %>
  </ul>

</div>

<%= link_to 'Add New Journal', new_journal_path %>